import React from "react";

function TeamLeadHome(props: any) {
  // we will pass the auth object as the current user
  const currentUser = props.currentUser;

  return <div>TeamLeadHome</div>;
}

export default TeamLeadHome;
